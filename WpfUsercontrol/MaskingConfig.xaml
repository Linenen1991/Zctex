<Window x:Class="WpfUsercontrol.MaskingConfig"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfUsercontrol.ViewModels"
        xmlns:behaviors="clr-namespace:WpfUsercontrol.Behaviors"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MaskingConfigViewModel, IsDesignTimeCreatable=True}"
        Title="MaskingConfig"
        Height="700"
        Width="1100">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="4*" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="SearchRuleTable"
                  Grid.Row="0"
                  ItemsSource="{Binding SearchRules}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  HeadersVisibility="Column"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="searchPatternToRed"
                                    Binding="{Binding SearchPatternToRed, UpdateSourceTrigger=PropertyChanged}"
                                    Width="2*" />
                <DataGridTextColumn Header="searchPatternToDarkRed"
                                    Binding="{Binding SearchPatternToDarkRed, UpdateSourceTrigger=PropertyChanged}"
                                    Width="2*" />
                <DataGridTemplateColumn Header="Prefix + [FixedString] + Suffix" Width="4*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <DockPanel LastChildFill="False">
                                <TextBox Width="160"
                                         Margin="0,0,8,0"
                                         VerticalContentAlignment="Center"
                                         Text="{Binding Prefix, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock Margin="0,0,8,0"
                                           VerticalAlignment="Center"
                                           FontWeight="SemiBold"
                                           Text="{Binding FixedCandidate}" />
                                <TextBox Width="160"
                                         VerticalContentAlignment="Center"
                                         Text="{Binding Suffix, UpdateSourceTrigger=PropertyChanged}" />
                            </DockPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="FixedString" Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.FixedCandidates, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding FixedCandidate, UpdateSourceTrigger=PropertyChanged}"
                                      VerticalContentAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="1" Margin="0,0,0,10" LastChildFill="False">
            <Button Content="Start Match"
                    Width="120"
                    Margin="0,0,10,0"
                    Command="{Binding StartMatchCommand}" />
            <TextBox x:Name="FilterTextBox"
                     Width="260"
                     Margin="0,0,10,0"
                     VerticalContentAlignment="Center"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
            <Button x:Name="FilterButton"
                    Content="Filter"
                    Width="100"
                    Command="{Binding FilterCommand}" />
        </DockPanel>

        <ListView x:Name="LogListView"
                  Grid.Row="2"
                  ItemsSource="{Binding DataFromServerView}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="UserComment" Width="220" DisplayMemberBinding="{Binding UserComment}" />
                    <GridViewColumn Header="Message" Width="820">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock TextWrapping="Wrap"
                                           behaviors:TextBlockInlinesBehavior.Segments="{Binding MessageSegments}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
